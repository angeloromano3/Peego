package br.com.fiap.peego.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta baseada no design definido pelo grupo (telas do Figma)
val VerdePrimario = Color(0xFF0D5C53)      // verde-petróleo escuro - botões, título, ícones principais
val VerdePrimarioEscuro = Color(0xFF094A43)  // hover/pressed
val FundoMenta = Color(0xFFE9F6F1)         // fundo verde-água claro
val CardBranco = Color(0xFFFFFFFF)         // cards
val BordaClara = Color(0xFFE0E0E0)         // borda dos inputs
val TextoPrimario = Color(0xFF1B1B1B)
val TextoSecundario = Color(0xFF5F5F5F)
val VerdeStatus = Color(0xFF2E7D5B)        // pin "ok" no mapa (tela Home, futura)
val AmareloStatus = Color(0xFFE0A72E)      // pin "com relato" no mapa (tela Home, futura)